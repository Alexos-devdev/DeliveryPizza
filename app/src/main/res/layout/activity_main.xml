<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:layout_margin="20dp">

    <!-- Barra superior con botón de carrito a la izquierda -->
    <ImageButton
        android:id="@+id/btnCarrito"
        android:layout_width="77dp"
        android:layout_height="55dp"
        android:layout_margin="8dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/img" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sabores de Pizza"
        android:textSize="18sp"
        android:textStyle="bold"
        android:padding="10dp"
        android:textAlignment="center"/>



    <!-- RecyclerView para mostrar las pizzas debajo del botón -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_pizzas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp" />

    <!-- Texto para mostrar información del producto -->
    <TextView
        android:id="@+id/txtResultado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginTop="10dp" />

    <!-- Botón para comprar, oculto inicialmente -->


</androidx.appcompat.widget.LinearLayoutCompat>
